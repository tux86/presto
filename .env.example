# All variables are optional — Presto runs with zero configuration.

# PORT=3001                  # HTTP port (default 3001, 8080 in Docker)
# DATA_DIR=./data            # Where presto.db lives
# APP_NAME=Presto            # Name shown in the UI
