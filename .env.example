# Database
DATABASE_URL=postgresql://presto:presto_dev_password@localhost:5432/presto

# Docker Compose PostgreSQL (only used by docker-compose.yml, not by the app)
# POSTGRES_USER=presto
# POSTGRES_PASSWORD=presto_dev_password
# POSTGRES_DB=presto
# POSTGRES_PORT=5432

# Backend
JWT_SECRET=<REPLACE_WITH_RANDOM_SECRET_MIN_32_CHARS>
CORS_ORIGINS=http://localhost:5173
# AUTH_DISABLED=true             # Set to disable authentication (single-user mode)
REGISTRATION_ENABLED=true
# RATE_LIMIT_MAX=20
# RATE_LIMIT_WINDOW_MS=900000
# TRUST_PROXY=false              # Set to true when behind a reverse proxy (nginx, Caddy, etc.)

# App
APP_NAME=Presto
# DEMO_DATA=false

# Defaults for new users
# DEFAULT_THEME=light             # light, dark, auto
# DEFAULT_LOCALE=en               # en, fr, de, es, pt
# DEFAULT_BASE_CURRENCY=EUR       # ISO 4217 currency code
